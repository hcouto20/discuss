<%= form_for @changeset, topic_path(@conn, :update, @topic), fn f -> %>
  <div class="form-group">
    <%= text_input f, :title, placeholder: "Título", class: "form-control" %>
    <%= error_tag f, :title %>
  </div>

  <%= submit "Salvar Tópico", class: "btn btn-primary" %>
<% end %>
