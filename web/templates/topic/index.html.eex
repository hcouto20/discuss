<h5>Tópicos</h5>

<ul class="collection">
  <%= for topic <- @topics do %>
    <li class="collection-item">
      <%= topic.id %>

        <i class="material-icons">_</i>

      <%= topic.title %>

      <div class="right">
        <%= link "Editar", to: topic_path(@conn, :edit, topic) %>

        <%= link "Excluir", to: topic_path(@conn, :delete, topic), method: :delete %>
      </div>
    </li>
  <% end %>
</ul>

<div class="fixed-action-btn">
  <%= link to: topic_path(@conn, :new), class: "btn-floating btn-large waves-effect waves-light red" do %>
    <i class="material-icons">add</i>
  <% end %>
  </a>
</div>
